<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShowOff Life - Subscription Management</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <%- include('partials/admin-styles') %>
    <style>
        .subscription-hero {
            background: linear-gradient(135deg, #8B5CF6 0%, #7C3AED 100%);
            color: white;
            padding: 3rem 2rem;
            border-radius: 16px;
            margin-bottom: 2rem;
            text-align: center;
        }

        .subscription-hero h2 {
            font-size: 2.5rem;
            font-weight: 700;
            margin: 0 0 1rem 0;
        }

        .subscription-hero p {
            font-size: 1.1rem;
            opacity: 0.95;
            margin: 0;
        }

        .plan-showcase {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
        }

        .plan-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1.5rem;
        }

        .plan-badge {
            width: 70px;
            height: 70px;
            border: 3px solid #8B5CF6;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            color: #8B5CF6;
            font-size: 12px;
        }

        .plan-status {
            background: #8B5CF6;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .plan-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: #1e293b;
            margin: 0 0 0.5rem 0;
        }

        .plan-subtitle {
            font-size: 0.9rem;
            color: #64748b;
            margin: 0;
        }

        .plan-price {
            font-size: 2.5rem;
            font-weight: 700;
            color: #8B5CF6;
            margin: 1.5rem 0 0.5rem 0;
        }

        .plan-price-period {
            font-size: 1rem;
            color: #64748b;
            font-weight: 400;
        }

        .plan-benefits {
            margin: 2rem 0;
            padding: 1.5rem;
            background: #f8fafc;
            border-radius: 12px;
        }

        .benefit-item {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            font-size: 0.95rem;
            color: #1e293b;
        }

        .benefit-item:last-child {
            margin-bottom: 0;
        }

        .benefit-icon {
            color: #10B981;
            margin-right: 0.75rem;
            font-size: 1.2rem;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-mini-card {
            background: white;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            text-align: center;
        }

        .stat-mini-value {
            font-size: 2rem;
            font-weight: 700;
            color: #8B5CF6;
            margin: 0.5rem 0;
        }

        .stat-mini-label {
            font-size: 0.85rem;
            color: #64748b;
            margin: 0;
        }

        .subscriptions-table {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .table-title {
            padding: 1.5rem;
            border-bottom: 1px solid #e2e8f0;
            font-size: 1.1rem;
            font-weight: 600;
            color: #1e293b;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <%- include('partials/admin-sidebar', { currentPage: 'subscriptions' }) %>

        <main class="main-content">
            <%- include('partials/admin-header', { pageTitle: 'Subscription Management' }) %>

            <div class="content-area">
                <!-- Hero Section -->
                <div class="subscription-hero">
                    <h2>Subscription Overview</h2>
                    <p>Monitor your subscription plans and user engagement</p>
                </div>

                <!-- Quick Stats -->
                <div class="stats-grid">
                    <div class="stat-mini-card">
                        <i class="fas fa-users" style="font-size: 1.5rem; color: #8B5CF6;"></i>
                        <div class="stat-mini-value"><%= stats.count || 0 %></div>
                        <p class="stat-mini-label">Active Subscribers</p>
                    </div>
                    
                    <div class="stat-mini-card">
                        <i class="fas fa-dollar-sign" style="font-size: 1.5rem; color: #10B981;"></i>
                        <div class="stat-mini-value">₹<%= ((stats.totalRevenue || 0) / 100).toLocaleString() %></div>
                        <p class="stat-mini-label">Total Revenue</p>
                    </div>
                    
                    <div class="stat-mini-card">
                        <i class="fas fa-layer-group" style="font-size: 1.5rem; color: #F59E0B;"></i>
                        <div class="stat-mini-value"><%= plans.length %></div>
                        <p class="stat-mini-label">Available Plans</p>
                    </div>
                    
                    <div class="stat-mini-card">
                        <i class="fas fa-chart-line" style="font-size: 1.5rem; color: #3B82F6;"></i>
                        <div class="stat-mini-value">₹<%= stats.count > 0 ? ((stats.totalRevenue || 0) / stats.count / 100).toFixed(0) : 0 %></div>
                        <p class="stat-mini-label">Avg Revenue/User</p>
                    </div>
                </div>

                <!-- Main Subscription Plan (Pro Plan) -->
                <% 
                    const proPlan = plans && plans.find(p => p.tier === 'pro');
                    const otherPlans = plans && plans.filter(p => p.tier !== 'pro');
                %>
                
                <% if (proPlan) { %>
                    <div class="plan-showcase" style="border: 3px solid #8B5CF6; position: relative;">
                        <div style="position: absolute; top: -12px; right: 20px; background: linear-gradient(135deg, #8B5CF6, #7C3AED); color: white; padding: 0.5rem 1rem; border-radius: 20px; font-size: 0.85rem; font-weight: 600;">
                            ⭐ Featured Plan
                        </div>

                        <div class="plan-header">
                            <div>
                                <div class="plan-badge"><%= proPlan.tier.toUpperCase().substring(0, 3) %></div>
                            </div>
                            <% if (proPlan.subscriberCount > 0) { %>
                                <div class="plan-status">
                                    <i class="fas fa-check-circle"></i> <%= proPlan.subscriberCount %> Active
                                </div>
                            <% } %>
                        </div>

                        <div style="margin-bottom: 1rem;">
                            <h3 class="plan-title"><%= proPlan.name %></h3>
                            <p class="plan-subtitle"><%= proPlan.description || 'Premium subscription plan' %></p>
                        </div>

                        <div style="margin: 1.5rem 0;">
                            <div class="plan-price">
                                ₹<%= (proPlan.price ? (proPlan.price / 100).toLocaleString() : '0') %>
                                <span class="plan-price-period">/<%= proPlan.duration || 30 %> days</span>
                            </div>
                        </div>

                        <div class="plan-benefits">
                            <% if (proPlan.features && proPlan.features.length > 0) { %>
                                <% proPlan.features.forEach(feature => { %>
                                    <div class="benefit-item">
                                        <i class="fas fa-check benefit-icon"></i>
                                        <span><%= feature %></span>
                                    </div>
                                <% }) %>
                            <% } else if (proPlan.highlightedFeatures && proPlan.highlightedFeatures.length > 0) { %>
                                <% proPlan.highlightedFeatures.forEach(feature => { %>
                                    <div class="benefit-item">
                                        <i class="fas fa-check benefit-icon"></i>
                                        <span><%= feature %></span>
                                    </div>
                                <% }) %>
                            <% } %>
                        </div>

                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 1rem; padding-top: 1rem; border-top: 1px solid #e2e8f0;">
                            <div style="text-align: center;">
                                <div style="font-size: 1.5rem; font-weight: 700; color: #8B5CF6;"><%= proPlan.subscriberCount || 0 %></div>
                                <div style="font-size: 0.85rem; color: #64748b;">Subscribers</div>
                            </div>
                            <div style="text-align: center;">
                                <div style="font-size: 1.5rem; font-weight: 700; color: #10B981;">₹<%= proPlan.subscriberCount > 0 ? ((proPlan.subscriberCount * proPlan.price) / 100).toLocaleString() : 0 %></div>
                                <div style="font-size: 0.85rem; color: #64748b;">Plan Revenue</div>
                            </div>
                            <div style="text-align: center;">
                                <div style="font-size: 1.5rem; font-weight: 700; color: #F59E0B;"><%= proPlan.isActive ? 'Active' : 'Inactive' %></div>
                                <div style="font-size: 0.85rem; color: #64748b;">Status</div>
                            </div>
                        </div>
                    </div>
                <% } %>

                <!-- Other Subscription Plans Display (Like Mobile) -->
                <% if (otherPlans && otherPlans.length > 0) { %>
                    <div style="margin-top: 2rem; margin-bottom: 1rem;">
                        <h3 style="color: #1e293b; font-size: 1.2rem; font-weight: 600;">Other Plans</h3>
                    </div>
                    <% otherPlans.forEach(plan => { %>
                        <div class="plan-showcase">
                            <div class="plan-header">
                                <div>
                                    <div class="plan-badge"><%= plan.tier.toUpperCase().substring(0, 3) %></div>
                                </div>
                                <% if (plan.subscriberCount > 0) { %>
                                    <div class="plan-status">
                                        <i class="fas fa-check-circle"></i> <%= plan.subscriberCount %> Active
                                    </div>
                                <% } %>
                            </div>

                            <div style="margin-bottom: 1rem;">
                                <h3 class="plan-title"><%= plan.name %></h3>
                                <p class="plan-subtitle"><%= plan.description || 'Premium subscription plan' %></p>
                            </div>

                            <div style="margin: 1.5rem 0;">
                                <div class="plan-price">
                                    ₹<%= (plan.price ? (plan.price / 100).toLocaleString() : '0') %>
                                    <span class="plan-price-period">/<%= plan.duration || 30 %> days</span>
                                </div>
                            </div>

                            <div class="plan-benefits">
                                <% if (plan.features && plan.features.length > 0) { %>
                                    <% plan.features.forEach(feature => { %>
                                        <div class="benefit-item">
                                            <i class="fas fa-check benefit-icon"></i>
                                            <span><%= feature %></span>
                                        </div>
                                    <% }) %>
                                <% } else if (plan.highlightedFeatures && plan.highlightedFeatures.length > 0) { %>
                                    <% plan.highlightedFeatures.forEach(feature => { %>
                                        <div class="benefit-item">
                                            <i class="fas fa-check benefit-icon"></i>
                                            <span><%= feature %></span>
                                        </div>
                                    <% }) %>
                                <% } %>
                            </div>

                            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 1rem; padding-top: 1rem; border-top: 1px solid #e2e8f0;">
                                <div style="text-align: center;">
                                    <div style="font-size: 1.5rem; font-weight: 700; color: #8B5CF6;"><%= plan.subscriberCount || 0 %></div>
                                    <div style="font-size: 0.85rem; color: #64748b;">Subscribers</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 1.5rem; font-weight: 700; color: #10B981;">₹<%= plan.subscriberCount > 0 ? ((plan.subscriberCount * plan.price) / 100).toLocaleString() : 0 %></div>
                                    <div style="font-size: 0.85rem; color: #64748b;">Plan Revenue</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 1.5rem; font-weight: 700; color: #F59E0B;"><%= plan.isActive ? 'Active' : 'Inactive' %></div>
                                    <div style="font-size: 0.85rem; color: #64748b;">Status</div>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } %>

                <% if (!plans || plans.length === 0) { %>
                    <div class="plan-showcase" style="text-align: center; padding: 3rem;">
                        <i class="fas fa-inbox" style="font-size: 3rem; color: #cbd5e1; margin-bottom: 1rem;"></i>
                        <p style="color: #64748b; margin: 0;">No subscription plans found</p>
                    </div>
                <% } %>

                <!-- Recent Subscriptions Table -->
                <div class="subscriptions-table" style="margin-top: 2rem;">
                    <div class="table-title">
                        <i class="fas fa-history"></i> Recent Subscriptions
                    </div>
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>User</th>
                                <th>Plan</th>
                                <th>Amount</th>
                                <th>Start Date</th>
                                <th>End Date</th>
                                <th>Status</th>
                                <th>Days Left</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (recentSubscriptions && recentSubscriptions.length > 0) { %>
                                <% recentSubscriptions.forEach(sub => { %>
                                    <% 
                                        const endDate = new Date(sub.endDate);
                                        const today = new Date();
                                        const daysLeft = Math.ceil((endDate - today) / (1000 * 60 * 60 * 24));
                                        const userDisplayName = sub.user ? sub.user.displayName : 'Unknown User';
                                        const userUsername = sub.user ? sub.user.username : 'unknown';
                                        const userProfilePicture = sub.user && sub.user.profilePicture ? (sub.user.profilePicture.startsWith('http') ? sub.user.profilePicture : 'http://localhost:3000' + sub.user.profilePicture) : 'data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2240%22 height=%2240%22%3E%3Crect fill=%22%23ddd%22 width=%2240%22 height=%2240%22/%3E%3Ctext x=%2250%25%22 y=%2250%25%22 text-anchor=%22middle%22 dy=%22.3em%22 fill=%22%23999%22 font-family=%22sans-serif%22 font-size=%2216%22%3E?%3C/text%3E%3C/svg%3E';
                                        const planName = sub.plan ? sub.plan.name : 'Unknown Plan';
                                        const planTier = sub.plan ? sub.plan.tier : 'basic';
                                    %>
                                    <tr>
                                        <td>
                                            <div class="user-profile">
                                                <img src="<%= userProfilePicture %>" alt="<%= userDisplayName %>" class="user-avatar" onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2240%22 height=%2240%22%3E%3Crect fill=%22%23ddd%22 width=%2240%22 height=%2240%22/%3E%3Ctext x=%2250%25%22 y=%2250%25%22 text-anchor=%22middle%22 dy=%22.3em%22 fill=%22%23999%22 font-family=%22sans-serif%22 font-size=%2216%22%3E?%3C/text%3E%3C/svg%3E'">
                                                <div class="user-info">
                                                    <h4><%= userDisplayName %></h4>
                                                    <p>@<%= userUsername %></p>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            <div style="font-weight: 600;"><%= planName %></div>
                                            <span class="badge" style="background: <%= planTier === 'premium' ? '#f59e0b' : planTier === 'vip' ? '#8b5cf6' : '#3b82f6' %>; color: white;">
                                                <%= planTier.toUpperCase() %>
                                            </span>
                                        </td>
                                        <td>
                                            <div style="font-weight: 600; color: #10b981;">
                                                ₹<%= (sub.amountPaid / 100).toLocaleString() %>
                                            </div>
                                        </td>
                                        <td>
                                            <div><%= new Date(sub.startDate).toLocaleDateString() %></div>
                                        </td>
                                        <td>
                                            <div><%= new Date(sub.endDate).toLocaleDateString() %></div>
                                        </td>
                                        <td>
                                            <span class="status-badge status-<%= sub.status %>">
                                                <%= sub.status.toUpperCase() %>
                                            </span>
                                        </td>
                                        <td>
                                            <div style="font-weight: 600; <%= daysLeft <= 7 ? 'color: #ef4444;' : 'color: #10b981;' %>">
                                                <%= daysLeft > 0 ? daysLeft + ' days' : 'Expired' %>
                                            </div>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="7" class="loading">
                                        <i class="fas fa-inbox"></i>
                                        <p>No subscriptions found</p>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
